package scope;

public class scope {
    public static void main(String[] args) {
       /*  anything initialise outside the box you can definetily use it inside the box it
        will be available inside the box you will update it inside the box 
        original one is get also updated as it is using the same reference variable
        and you cannot reinitialise in the box 
        //anything which is initialised inside a box of scope you can definetly insitialise it outside 
        but you cannot use it outside 
        it work same in loops also

         */
    }
}
